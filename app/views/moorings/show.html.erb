
<%= render 'shared/navbar-search' %>
<div class="banner-mooring" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)),
           url(<%= cl_image_path('mooring1', gravity: 'north') %>)">
  <div class="card-banner">
    <h1 class="mooring-header"><%= @mooring.name %></h1>
    <h4 id="label-mooring-status">available</h4>
  </div>
</div>

  <div class="container">
    <div class="row">
      <div class="container-booking-info">
        <div class="col-xs-12 col-sm-4">
          <div id="map" style="width:400px;height:400px" data-markers="<%= @markers.to_json %>"></div>
        </div>
        <div class="col-xs-12 col-sm-4 col-sm-offset-2">
          <div class="form-datepicker">
            <h2>Pick a date</h2>
            <%= simple_form_for [@mooring, @booking] do |f| %>
              <%= f.input :start_date, as: :string, required: false, input_html: {class: "flatpickr"} %>
              <%= f.input :end_date, as: :string, required: false, input_html: {class: "flatpickr"} %>
              <span class="button-datepicker">
                 <%= f.button :submit, "Submit", class: "submit-mooring-date" %>
              </span>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>

<div class="booking-rules-container">
  <h4>Useful information</h4>
  <ul>
    <li>£10 per night per boat (payment is taken at the time of the booking)</li>
    <li>Booking can be made online or through the customer call centre</li>
    <li>Bookings can be made for 1-7 days (max 7 days in a single visit)</li>
    <li>Arrival time after 1pm, departure time by 12 noon</li>
    <li>Bookings are made through the web licensing system so you will need to have an existing account (or set one up)</li>
    <li>Bookings will be confirmed by email</li>
    <li>Bookings can be changed or cancelled by you up to 14 days before the start of the booking period, or by calling the customer service centre if within 14 days of the start of the booking</li>
    <li>An unpermitted use charge of £25 per day applies to boats that have not booked to use the moorings</li>
  </ul>
  <p>Read the full terms and conditions for moorings or the plain English explanation of the terms and conditions.</p>
</div>
